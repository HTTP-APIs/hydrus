hydrus:
  restart: always
  build: ./
  ports:
    - "8080:80"
  environment:
    - HYDRUS_SERVER_URL=http://54.169.232.177:8080/
  links:
    - postgres:postgres

postgres:
  restart: always
  environment:
    - POSTGRES_USER=hydra
    - POSTGRES_PASSWORD=hailhydra
    - POSTGRES_DB=hydrus
  image: postgres:latest
  ports:
    - "9000:5432"
